<%= link_to 'New User liability', new_user_liability_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mb-4 inline-block" %>

<table class="min-w-full bg-white border border-gray-200">
  <thead>
    <tr>
      <th class="py-2 px-4 border-b">Item Name</th>
      <th class="py-2 px-4 border-b">Purchase Price (Cents)</th>
      <th class="py-2 px-4 border-b">Purchase Price Currency</th>
      <th class="py-2 px-4 border-b">Purchase Date</th>
      <th class="py-2 px-4 border-b" colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @user_liabilities.each do |user_liability| %>
      <tr class="hover:bg-gray-100">
        <td class="py-2 px-4 border-b"><%= user_liability.item_name %></td>
        <td class="py-2 px-4 border-b"><%= user_liability.purchase_price_cents %></td>
        <td class="py-2 px-4 border-b"><%= user_liability.purchase_price_currency %></td>
        <td class="py-2 px-4 border-b"><%= user_liability.purchase_date %></td>
        <td class="py-2 px-4 border-b"><%= link_to 'Show', user_liability, class: "text-blue-500 hover:text-blue-700" %></td>
        <td class="py-2 px-4 border-b"><%= link_to 'Edit', edit_user_liability_path(user_liability), class: "text-yellow-500 hover:text-yellow-700" %></td>
        <td class="py-2 px-4 border-b"><%= link_to 'Destroy', user_liability, method: :delete, data: { confirm: 'Are you sure?' }, class: "text-red-500 hover:text-red-700" %></td>
      </tr>
    <% end %>
  </tbody>
  <tfoot>
    <tr>
      <td colspan="1" class="py-2 px-4 border-t font-bold">Total</td>
      <td class="py-2 px-4 border-t"><%= @total_liabilities %></td>
      <td colspan="5" class="border-t"></td>
    </tr>
  </tfoot>
</table>