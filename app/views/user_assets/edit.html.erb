<h1>UserAssets#edit</h1>

<%= form_with(model: @user_asset, local: true) do |form| %>
  <% if @user_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_asset.errors.count, "error") %> prohibited this user_asset from being saved:</h2>

      <ul>
        <% @user_asset.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item_name %>
    <%= form.text_field :item_name %>
  </div>

  <div class="field">
    <%= form.label :purchase_price_cents %>
    <%= form.number_field :purchase_price_cents %>
  </div>

  <div class="field">
    <%= form.label :purchase_price_currency %>
    <%= form.text_field :purchase_price_currency %>
  </div>

  <div class="field">
    <%= form.label :purchase_date %>
    <%= form.date_select :purchase_date %>
  </div>

  <div class="actions">
    <%= form.submit "Save changes" %>
  </div>
<% end %>

<%= link_to 'Back', @user_asset %>